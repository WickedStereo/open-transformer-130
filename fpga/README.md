# FPGA placeholder flow

Set the target part and package when running the starter iCE40 flow:

```bash
make fpga ICE40_ARCH=up5k ICE40_PACKAGE=sg48
```

Add board-specific constraints and top-level wrappers here as the project matures.
